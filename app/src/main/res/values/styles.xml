<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Dark Application theme -->
    <style name="DarkTheme" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">#031d1b</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/cardview_light_background</item>
        <item name="android:textSize">20dp</item>
        <item name="android:windowBackground">@color/colorBackgroundWindowDark</item>
    </style>

    <!-- Dark Application theme -->
    <style name="LightTheme" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/colorPrimaryDark</item>
        <item name="colorPrimaryDark">#031d1b</item>
        <item name="colorAccent">#dad5b3</item>
        <item name="android:textColor">#263238</item>
        <item name="android:windowBackground">@color/colorBackgroundWindowLight</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="LightTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="LightTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>
